@page "/"
@inject NavigationManager _nav

<div class="flex h-screen">
    <div class="flex flex-col w-full border-opacity-50 m-auto">
        <button class="btn btn-wide w-full btn-primary my-1">Diet</button>
        <div class="divider w-full text-secondary">OR</div>
        <button class="btn btn-wide w-full btn-primary my-1" @onclick="@(()=>_nav.NavigateTo("/WorkoutMain"))">Workout</button>
    </div>
</div>

