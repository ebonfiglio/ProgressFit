<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProgressFit.Mobile.MainPage"
             xmlns:viewmodel="clr-namespace:ProgressFit.Mobile.ViewModels"
             x:DataType="viewmodel:MainPageViewModel">

    <ScrollView>
        <VerticalStackLayout
            Spacing="25"
            Padding="30,0"
            VerticalOptions="Center">

            <Label
                Text="ProgressFit"
                SemanticProperties.Description="ProgresFit logo."
                HeightRequest="200"
                HorizontalOptions="Center" />
            <Button
                x:Name="workoutBtn"
                Text="Workout"
                SemanticProperties.Hint="Go to workout center"
                Command="{Binding WorkoutBtnClickedCommand}"
                HorizontalOptions="Center" />
            <Button
                x:Name="dietBtn"
                Text="Diet"
                SemanticProperties.Hint="Go to diet center"
               
                HorizontalOptions="Center" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
